import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
   fullWidth: {
      resizeMode: 'contain',
      flex: 1,
      aspectRatio: 1,
   },
   user: {
      resizeMode: 'contain',
      width: 100,
      height: 100,
      aspectRatio: 1,
   },
})

export default styles
