import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
   fullWidth: {
      resizeMode: 'contain',
      flex: 1,
      aspectRatio: 1,
   },
   user: {
      maxWidth: 350,
      maxHeight: 350,
      resizeMode: 'contain',
      flex: 1,
      aspectRatio: 1,
   },
})

export default styles
